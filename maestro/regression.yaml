# Opens the Regression tab and waits for every case to report OK. A crash or a
# missing event fails the flow. Each case is scrolled into view first (a no-op
# when it's already visible; on the legacy architecture, iOS reports off-screen
# rows as visible, so the waits pass without scrolling there).
#
#   bunx maestro-runner --platform ios|android test -e APP_ID=<app id> maestro/regression.yaml
#
# App IDs are listed in walkthrough.yaml.
appId: ${APP_ID}
# maestro-runner saves each screenshot in the run's report and also here,
# relative to this file (ignored).
env:
    OUT: maestro-screenshots
---
- launchApp:
      stopApp: true
# The first launch after an install can be slow on Android (the app is still
# being compiled), so wait for the tab bar rather than tapping right away.
- extendedWaitUntil:
      visible:
          id: 'tab-regression'
      timeout: 60000
- tapOn:
      id: 'tab-regression'
- scrollUntilVisible:
      element: 'tint-remote: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'tint-remote: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'tint-404: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'tint-404: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'remove-onLoad: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'remove-onLoad: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'remove-onLoadEnd: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'remove-onLoadEnd: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'remove-onError: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'remove-onError: OK'
      timeout: 30000
- takeScreenshot: ${OUT}/regression-top
- scrollUntilVisible:
      element: 'default-no-source: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'default-no-source: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'preload-no-uri: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'preload-no-uri: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'preload-unresolved: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'preload-unresolved: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'clear-tint: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'clear-tint: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'layout: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'layout: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'layout-fallback: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'layout-fallback: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'fallback-require: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'fallback-require: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'touchable: .*'
      timeout: 10000
- tapOn:
      id: 'regression-touchable-image'
- extendedWaitUntil:
      visible: 'touchable: OK'
      timeout: 10000
- scrollUntilVisible:
      element: 'tint-style: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'tint-style: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'pointer-events: .*'
      timeout: 10000
- tapOn:
      id: 'regression-pointer-events-target'
- extendedWaitUntil:
      visible: 'pointer-events: OK'
      timeout: 10000
- scrollUntilVisible:
      element: 'error-invalid-data-uri: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'error-invalid-data-uri: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'error-empty-uri: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'error-empty-uri: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'error-null-uri-default: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'error-null-uri-default: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'resize-mode: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'resize-mode: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'no-reload: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'no-reload: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'source-swap: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'source-swap: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'load-start-once: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'load-start-once: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'zero-size: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'zero-size: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'zero-height: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'zero-height: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'asset-uri: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'asset-uri: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'source-size: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'source-size: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'source-size-gif: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'source-size-gif: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'source-size-cached: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'source-size-cached: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'progress-unknown-size: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'progress-unknown-size: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'preload: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'preload: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'preload-headers: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'preload-headers: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'gif-loop-once: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'gif-loop-once: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'web-cache: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'web-cache: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'cookies: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'cookies: OK'
      timeout: 30000
- scrollUntilVisible:
      element: 'resize-center: .*'
      timeout: 10000
- extendedWaitUntil:
      visible: 'resize-center: OK'
      timeout: 30000
# Give any delayed crash (e.g. from a prop update after an event) time to happen.
- waitForAnimationToEnd:
      timeout: 3000
- assertVisible: 'preload-headers: OK'
- takeScreenshot: ${OUT}/regression
